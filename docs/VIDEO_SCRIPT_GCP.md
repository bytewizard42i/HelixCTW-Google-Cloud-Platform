# HelixCTW Google Cloud TestWired video script

- **Target runtime:** 2:45–2:55
- **Demo:** <https://helixctw.com> → Google Cloud card → <https://helixctw-gcp-testwired.vercel.app>
- **Narrator:** Gemini `gemini-3.1-flash-tts-preview`, Charon voice
- **Output:** a new video only; never overwrite an existing render

## Claim boundary

The recording must show and say the evidence exactly as deployed:

- Google Cloud Run and CockroachDB: `REALDEAL_TEST`, `CONNECTED`.
- Midnight: `SOURCE_ONLY`, `NOT_CONNECTED`. Midnight is highlighted as the
  minimum-disclosure proof architecture and next live proof adapter—not falsely
  described as a network execution in this build.
- DIDz, AgenticDID, and RWAz: deterministic `MOCK` fixtures.
- Gemini Charon: pre-generated narration, not an application reasoning model.
- Terraform: real applied infrastructure-as-code, image pinned by digest.

## Timed narration and visuals

### 0:00–0:14 — Three clouds, one weave

**Visual:** HelixCTW landing page, then choose the Google Cloud card.

> This is HelixCTW on Google Cloud: a privacy-preserving memory and proof weave
> for AI agents. The same protocol can move across clouds without moving its
> trust boundary.

### 0:14–0:34 — Terraform makes the GCP edition reproducible

**Visual:** clean animated GCP architecture card, then short readable Terraform
snippets: Cloud Run, Artifact Registry, GCS, Secret Manager, IAM, image digest.

> Terraform declares the complete Google Cloud edition as reviewed code:
> Artifact Registry holds an immutable container, Cloud Run executes it, Secret
> Manager protects database credentials, Cloud Storage keeps audit receipts,
> and least-privilege IAM connects only the required pieces. Plan, review,
> apply—and then verify that reality has no drift.

### 0:34–0:50 — Google Cloud and Midnight have different jobs

**Visual:** split architecture: Google Cloud live runtime on the left; Midnight
privacy/proof boundary on the right. Show honest provider labels.

> Google Cloud carries the live runtime. Midnight defines the privacy boundary:
> prove the permitted fact, not the private record. In this TestWired build,
> Google Cloud and CockroachDB are live. Midnight remains visibly source-only
> until a real network proof adapter earns promotion.

### 0:50–1:08 — Live evidence gate

**Visual:** GCP judge console header, `LIVE TESTWIRED`, provider matrix, release
commit, and Start Guided Demo with Gemini Charon label.

> The browser trusts no green animation. Health, status, and the synthetic case
> catalog must agree on one release. Only then does the seven-checkpoint journey
> unlock. Every response is bound to its raw Cloud Run request identifier.

### 1:08–1:28 — Session A becomes durable memory

**Visual:** Checkpoint 1 and Checkpoint 2; open Session A, then close and forget.
Show run/session IDs and CockroachDB receipt.

> Session A opens inside a serializable CockroachDB transaction. When the
> browser closes and forgets the conversation, CockroachDB retains only bounded,
> public-safe memory and its evidence—not the private deed.

### 1:28–1:49 — Vector recall in fresh Session B

**Visual:** Checkpoint 3; show two returned matches, source session, memory ID,
projection generation, and semantic distance.

> A fresh Session B asks where the work stopped. CockroachDB Distributed Vector
> Indexing pins the run and projection as exact prefixes, ranks by cosine
> distance, and returns two bounded matches. The stored vector and commitment
> never leave the database.

### 1:49–2:08 — Minimum disclosure

**Visual:** Checkpoint 4; show `value: true`, source text disclosed `false`,
protected fields `0`, and Midnight `SOURCE_ONLY` in the matrix.

> The permitted question asks whether the fictional property is unencumbered.
> The contract returns one authorized bit: true. No deed text, mortgage record,
> owner identity, or private contact data. This is the Midnight principle in
> practice—even while the network adapter remains honestly source-only.

### 2:08–2:25 — Memory cannot become permission

**Visual:** Checkpoint 5 disclosure attempt and DENIED receipt.

> Now an unauthorized agent asks for every protected field. The request is
> denied, zero protected fields are returned, and the refusal becomes a durable
> receipt. Memory can recover context. It cannot manufacture authority.

### 2:25–2:43 — Rebuild and continuity

**Visual:** Checkpoints 6 and 7; projection rebuilt, commitment verified, same
predicate/value through the new active generation.

> HelixCTW rebuilds a disposable recall projection from the same canonical
> corpus. The evidence commitment remains stable. Through the new generation,
> the same memory yields the same predicate and the same value—continuity
> verified without rewriting canonical history.

### 2:43–2:55 — Inspectable close

**Visual:** retrieve the final receipt, then end card with Google Cloud,
Terraform, Midnight, CockroachDB, repository and demo URL.

> The final receipt binds the stored operation to its original Google Cloud
> request. Cloud Run executes. CockroachDB remembers. Terraform reproduces.
> Midnight keeps the proof boundary narrow. HelixCTW: prove one thing, and
> nothing more.

## End-card copy

- **HELIXCTW — GOOGLE CLOUD TESTWIRED**
- Cloud Run · Terraform · CockroachDB · Midnight
- `helixctw.com`
- `helixctw-gcp-testwired.vercel.app`

*Live TestWired software. Synthetic data only. Midnight adapter: SOURCE_ONLY.*
